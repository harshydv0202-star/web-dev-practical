import React from "react";
import Child from "./Child";

function App() {
  // Parent variables
  const name = "harsh";
  const age = 19;
  const hobby = ["Cricket", "Coding", "Gym"];

  return (
    <div>
      <h1>Parent Component</h1>

      {/* Passing props to child */}
      <Child name={name} age={age} hobby={hobby} />
    </div>
  );
}

export default App;
